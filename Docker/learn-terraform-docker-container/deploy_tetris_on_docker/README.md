# Deploy Tetris game on docker

### Initialize the terraform configuration
`terraform init`


### Format and validate the configuration
`terraform fmt`

`terraform validate`

### Create the execution plan
`terraform plan`

### Create the infrastructure
`terraform apply`


### Inspect state
`terraform show`

### State list
`terraform state list`


